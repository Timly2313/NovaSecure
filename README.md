# RUN THE APP

npm install

npm start

# 🖥️ Backend Setup – YOLO Object Detection with Webcam
cd yolo-backend

pip install -r requirements.txt

python webcam_server.py

# Find your computer's local IP
Open a new terminal and run:

bash
Windows
ipconfig | findstr "IPv4"

Mac / Linux
ifconfig | grep "inet "
Look for an address like 192.168.1.8 or 10.0.0.5.
